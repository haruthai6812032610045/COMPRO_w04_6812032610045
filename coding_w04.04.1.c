// กรณีที่ 1: ใช้ int เก็บเลขทศนิยม (ไม่ถูกต้อง)
#include <stdio.h>

int main() {
  int score = 40.5; //  ไม่เหมาะสม: int ไม่รองรับทศนิยม
    printf("Score = %d\n", score); //  จะแสดงผลไม่ถูกต้อง เช่น 40 หรือค่าไม่ถูกต้อง
    return 0;
}