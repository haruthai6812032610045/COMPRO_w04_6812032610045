// กรณีที่ 2: ใช้ float เก็บเลขทศนิยม (ถูกต้อง)
#include <stdio.h>

int main() {
    float score = 40.5; //float รองรับทศนิยม
    printf("Score = %.1f\n", score); //แสดงค่าที่ได้มาอย่างถูกต้อง: 40.5
    return 0;
}